@model List<MyPortfolio.DAL.Entities.About>

<section id="about" class="s-about target-section">

    @foreach (var item in Model)
    {
        <div class="row narrow section-intro has-bottom-sep">
            <div class="col-full text-center">
                <h3>Hakkımda</h3>
                <h1>@item.Title</h1>
                <p class="lead">@item.SubDescription</p>
            </div>
        </div>

        <div class="row about-content">

            <div class="col-full text-center">
                <h3>Selam!</h3>

                <p>
                    @item.Details
                </p>
            </div>

        </div>
    }

    <div class="row about-content about-content--buttons">

        <div class="col-six tab-full left">
            <a href="#0" class="btn btn--primary full-width">CV'mi İndir</a>
        </div>
        <div class="col-six tab-full right">
            <a href="#0" class="btn full-width">Benimle İletişime Geç</a>
        </div>

    </div>

    @await Component.InvokeAsync("_ExperiencePartial")

</section>
